#!/usr/bin/env bash
set -euo pipefail

# mise config hash: {{ include "dot_config/mise/config.toml" | sha256sum }}

echo "Installing mise packages..."
"$HOME/.local/bin/mise" install --yes 2>/dev/null || "$HOME/.local/bin/mise" install
